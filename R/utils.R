#' Class type
#'
#' @description
#' Its a list with `BType` and `CType` arguments:
#' * `BType = "B"`
#' * `CType = "C"`
#'
#' Used in [regexcite::ClassMaker] class as `type` parameter of `make` method
#'
#' @family utils
#' @export

utils.ClassType <- list(
  BType = "B",
  CType = "C"
)
